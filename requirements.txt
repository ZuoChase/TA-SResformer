# Install the PyTorch build matching your CUDA toolkit first when needed.
torch>=2.1

numpy>=1.24,<3
timm>=1.0,<2
spikingjelly==0.0.0.0.14

# The released neuron backend uses CuPy. Replace this with cupy-cuda11x on CUDA 11.
cupy-cuda12x>=13
